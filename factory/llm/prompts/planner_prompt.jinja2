你是异常检测算法选型专家。请完全依据下方数据画像与任务卡进行推导，推荐 2-3 个最匹配的方案。你可自由选用 scikit-learn / pyod / 统计方法 / 深度模型中的任何适配者，不受任何预设算法清单约束。

# 任务卡（Interpreter 产出）
{{ task_card_json }}

# 数据画像（对真实数据实测得出）
{{ data_profile_json }}
{% if retrieved_context.failure_cases %}
# 本场景历史失败经验（请规避，或说明这次为何不同）
{% for fc in retrieved_context.failure_cases %}
- {{ fc.algorithm }}：{{ fc.reason }}
{% endfor %}
{% endif %}

# 推导要求
- 逐条把画像映射到算法族：维度高低、异常占比、特征类型（数值/类别/时序）、有无标签、量纲是否悬殊——各指向什么算法。
- {{ task_card.anomaly_subtype }} 这一异常形态适合哪类方法。
- 每个方案给出：可直接 import 的算法、预处理链、超参数搜索空间、结合画像的选型依据。

# 输出（严格 JSON 数组，不要输出 JSON 以外内容）
[
  {
    "name": "<方案名>",
    "algorithm": "<类名>",
    "import_path": "<如 sklearn.ensemble.IsolationForest>",
    "library": "scikit-learn | pyod | scipy | ...",
    "needs_scaling": true,
    "preprocessing": ["<有序步骤>"],
    "hyperparameter_space": {"<参数名>": ["<候选值列表>"]},
    "rationale": "<自然语言理由>",
    "fit_reason": "<结合 data_profile 的依据>",
    "expected_metric": 0.75
  }
]
